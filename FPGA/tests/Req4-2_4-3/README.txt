REQ 4-2:4-3

pwm_tb.vhd (pwm.vhd, encoder_controller.vhd)

A testbench for making sure the suitable PWM output is given for work on the
motors, and to make sure the borrowed code works well with the program.